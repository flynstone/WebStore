using Microsoft.AspNetCore.Identity;

namespace Api.Models
{
    public class User : IdentityUser
    {
        // Not going to add any additional properties. 
        // Only using the derived IdentityUser properties.
    }
}
